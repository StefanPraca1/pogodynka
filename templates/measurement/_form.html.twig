{{ form_start(form) }}
  {{ form_row(form.location) }}
  {{ form_row(form.date) }}
  {{ form_row(form.celsius) }}
<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
